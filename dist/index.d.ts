import { LensApplication } from './application';
import { ApplicationConfig } from '@loopback/core';
export { LensApplication };
export declare function main(options?: ApplicationConfig): Promise<LensApplication>;
